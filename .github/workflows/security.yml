name: Security

on:
  push:
    branches: ["main", "claude/**"]
  pull_request:
  schedule:
    - cron: "0 6 * * 1" # weekly, Monday 06:00 UTC — catches newly disclosed CVEs

jobs:
  pip-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install project + dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pip-audit

      # --skip-editable: `pip install -e .` puts ReVision itself in the audited set, and
      # pip-audit looks it up on PyPI, where an unrelated project owns the name `revision`.
      # PyPI answers a missing version with 503 (not 404), which crashes the audit. Our
      # own package is not a dependency, so there is nothing of ours to audit there.
      - name: Audit Python dependencies
        run: pip-audit --skip-editable

  npm-audit:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: power-pack
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install power-pack Node deps
        run: npm ci

      - name: Audit Node dependencies
        run: npm audit --audit-level=high
