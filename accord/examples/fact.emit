# Recursion is admitted only with a measure that must shrink on every call.
def fact(n: Int [trust: 256]) -> Int
  measure: n
  require: not_void(n)
  if n <= 0:
    return 1
  else:
    return n * fact(n - 1)
example: fact(0) == 1 @ 120
example: fact(5) == 120 @ 120
