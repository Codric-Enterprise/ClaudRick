---
description: Reset the current approach and restart
argument-hint: [optional: new direction]
---
Set aside the approach taken so far and restart clean. Briefly confirm what
we're keeping (goal, constraints) versus discarding (the prior approach), then
begin fresh in the direction below.

> This resets the *approach*, not the transcript. To clear conversation context
> entirely, use Claude Code's built-in `/clear`.

New direction: $ARGUMENTS
