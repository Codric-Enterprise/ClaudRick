---
description: Write tests for code
argument-hint: [file path or function]
---
Write tests for the following. Cover the happy path, edge cases, and failure
modes. Match the project's existing test framework and layout (in this repo:
pytest under `tests/`, mirroring the module name). Make the tests deterministic
and independent, then note how to run them.

Target: $ARGUMENTS
