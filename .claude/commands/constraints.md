---
description: Set the constraints to respect
argument-hint: [limits: budget, time, tech, style, ...]
---
Take the following as hard constraints for the task. Restate them as a checklist,
note any that conflict with each other, and keep every proposed solution inside
them — call out explicitly if a good option would require breaking one.

Constraints: $ARGUMENTS
