---
description: Convert between formats
argument-hint: [content] to [target format]
---
Convert the following into the requested target format (e.g. JSON↔CSV↔YAML,
Markdown↔HTML, one language→another). Preserve all data and meaning, and flag
anything that can't map cleanly.

Request: $ARGUMENTS
