---
description: Work with an API
argument-hint: [API + what you want to do]
---
Help use the following API. Identify the right endpoint(s), required auth,
parameters, and response shape, then give a minimal working request example and
handle errors/rate limits. Prefer the official docs; don't invent endpoints or
fields — flag anything you're unsure of.

Request: $ARGUMENTS
