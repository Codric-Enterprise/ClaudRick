---
description: Clear focus and start a fresh topic
argument-hint: [optional: the new topic]
---
Drop the prior topic from active focus and start fresh on the new topic below
(or await it if none is given). Don't carry over assumptions from the previous
thread.

> In Claude Code, the built-in `/clear` actually wipes conversation context and
> takes precedence over this prompt helper — use the built-in for a true reset.

New topic: $ARGUMENTS
