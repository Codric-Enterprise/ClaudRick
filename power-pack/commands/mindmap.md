---
description: Build a mind map (Mermaid)
argument-hint: [central topic]
---
Build a mind map of the following as a Mermaid `mindmap` diagram in a fenced
```mermaid``` block: central node, main branches, and sub-branches. Keep labels
to a few words.

Topic: $ARGUMENTS
