---
description: Change the output format
argument-hint: [target format] [+ optional text]
---
Reformat into the requested shape below (e.g. table, bullets, numbered steps,
JSON, headings). Keep all content; change only presentation. If no text is
given, reformat your previous reply.

Request: $ARGUMENTS
