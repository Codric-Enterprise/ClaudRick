---
description: Organize information into a table
argument-hint: [what to tabulate + desired columns]
---
Organize the following into a clean Markdown table. Infer sensible columns if
they aren't given, keep cells terse, and don't invent data — leave unknown cells
blank.

Data: $ARGUMENTS
