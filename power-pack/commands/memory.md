---
description: Note something to remember for this project
argument-hint: [fact/preference to remember]
---
Remember the following for this project and apply it going forward. If it's a
durable project fact or convention, propose adding it to `CLAUDE.md` so it
persists across sessions, and show the exact line(s) you'd add.

> In Claude Code, the built-in `/memory` command edits memory files directly and
> takes precedence over this prompt helper.

To remember: $ARGUMENTS
