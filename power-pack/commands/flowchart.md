---
description: Draw a flowchart (Mermaid)
argument-hint: [process to diagram]
---
Draw the following process as a Mermaid `flowchart TD` in a fenced
```mermaid``` block. Include decision branches and end states. Keep node labels
short.

Process: $ARGUMENTS
