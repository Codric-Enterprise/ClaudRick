---
description: Scaffold a structured brief (Task/Context/Inputs/Constraints/Success/Output)
argument-hint: [what you want done — or blank to structure the current request]
---
Turn the request below into a well-structured brief before doing any work. Fill
in each element from what I've given you; where something is missing, either
state a sensible default or add it to the questions list — don't silently invent
requirements.

Produce the brief in exactly this shape:

- **Task** — the single outcome I want.
- **Context** — the background that changes how you'd approach it.
- **Inputs / files** — the material to work from (name what's provided and what's
  missing).
- **Constraints** — limits to respect (scope, tone, tech, length, deadlines).
- **Success criteria** — how we'll judge "done".
- **Output format** — the exact shape of the deliverable.

Then: **before starting**, list any clarifying questions you need answered. Only
begin the actual work once the brief is settled — if nothing is ambiguous, say so
and proceed.

Request: $ARGUMENTS
