---
description: Connect / integrate two tools
argument-hint: [tool A + tool B, and the goal]
---
Plan an integration between the following tools/systems. Define what data flows
which way, the connection method (API, webhook, MCP connector, file exchange),
auth, and failure handling. Give the concrete steps to wire it up and how to
test the round trip.

Integration: $ARGUMENTS
