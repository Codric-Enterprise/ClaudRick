---
description: Define an event trigger
argument-hint: [event → action]
---
Define a trigger for the following: the event that fires it, the condition to
match, and the action to take. Specify how the event is detected (webhook, file
watch, CI hook, poll), and add a guard against firing on the wrong thing or
firing twice.

Trigger: $ARGUMENTS
